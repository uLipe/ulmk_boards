diff --git a/src/jtag/drivers/tas_client/tas_client.c b/src/jtag/drivers/tas_client/tas_client.c
index 55294d6..c29d921 100644
--- a/src/jtag/drivers/tas_client/tas_client.c
+++ b/src/jtag/drivers/tas_client/tas_client.c
@@ -98,7 +98,7 @@ static int tas_client_reset(int trst, int srst) {
   }
   if (srst) {
     return tas_client_device_connect(client_state.sock,
-                                     TAS_DEV_CON_FEAT_RESET_AND_HALT);
+                                     TAS_DEV_CON_FEAT_RESET);
   }
 
   return 0;
@@ -342,6 +342,12 @@ static int tas_client_op_connect(struct aurix_ocds *ocds) {
     }
   }
 
+  if (target == NULL && client_state.target_num == 1) {
+    target = &client_state.targets[0];
+    LOG_WARNING("OCDS %s: using sole TAS target %s (device_type 0x%08x)",
+                ocds->name, target->identifier, target->device_type);
+  }
+
 out:
   if (target == NULL) {
     LOG_ERROR("No matching target for OCDS %s found", ocds->name);
